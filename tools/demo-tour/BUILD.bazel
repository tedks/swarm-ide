package(default_visibility = ["//visibility:public"])

filegroup(name = "sources", srcs = glob(["*.sh", "*.mjs", "*.cjs"]))

sh_test(
    name = "regressions",
    srcs = ["regressions.sh"],
    data = [":sources", "//tools/task-integration:sources", "//tools:electron-runtime-source"],
    tags = ["local"],
)

sh_binary(
    name = "smoke",
    srcs = ["smoke.sh"],
    data = [
        ":sources",
        "//:desktop-bundle",
        "//tools:virtual-desktop-run.sh",
        "//tools:x11-driver-source",
        "//tools:electron-runtime-source",
        "//tools/task-integration:sources",
        "//tools/demo-plans:sources",
    ],
    tags = ["manual"],
)
