package(default_visibility = ["//visibility:public"])

sh_binary(
    name = "probe",
    srcs = ["probe.sh"],
    data = glob(["*.mjs", "*.nix"]),
    tags = ["manual", "local"],
)

sh_test(
    name = "boundary-test",
    srcs = ["boundary.test.sh"],
    data = [":probe"],
    tags = ["local", "no-sandbox"],
    timeout = "moderate",
)
