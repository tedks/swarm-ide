// Reuse the exact-pane identity checks and single registry writer.
export { discover, metadata } from "../session-registration/identity";
export { updateRegistry } from "../session-registration/registry";
