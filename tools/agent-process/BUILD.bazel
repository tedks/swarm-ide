package(default_visibility = ["//visibility:public"])

sh_test(
    name = "proof-test",
    srcs = ["proof.sh"],
    data = ["//:quality_sources"],
    tags = ["local", "no-sandbox"],
    timeout = "moderate",
)
