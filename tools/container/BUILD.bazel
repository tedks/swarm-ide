package(default_visibility = ["//visibility:public"])

filegroup(name = "sources", srcs = glob(["**"], exclude = ["BUILD.bazel"]))

sh_test(
    name = "checks",
    srcs = ["checks.sh"],
    data = [":sources", "//:Dockerfile", "//:compose.yaml", "//:.dockerignore"],
    tags = ["local"],
)

sh_binary(
    name = "smoke",
    srcs = ["smoke.sh"],
    data = [":sources", "//:Dockerfile", "//:compose.yaml", "//:.dockerignore"],
    tags = ["manual", "local"],
)
