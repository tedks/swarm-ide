package(default_visibility = ["//visibility:public"])

filegroup(name = "sources", srcs = glob(["*.sh", "*.mjs", "*.cjs"]))

sh_binary(
    name = "smoke",
    srcs = ["smoke.sh"],
    data = [":sources", "//:desktop-bundle", "//tools:electron-runtime-source", "//tools:virtual-desktop-run.sh", "//tools:x11-driver-source", "//tools/demo-plans:sources", "//tools/task-integration:sources"],
    tags = ["manual"],
)

sh_test(
    name = "regressions",
    srcs = ["regressions.sh"],
    data = ["//:quality_sources"],
    tags = ["local"],
)
