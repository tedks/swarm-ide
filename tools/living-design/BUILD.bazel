package(default_visibility = ["//visibility:public"])
sh_test(
    name = "checks",
    srcs = ["checks.sh"],
    data = ["//:quality_sources", "//docs/design:sources"],
    tags = ["local"],
)
filegroup(name = "sources", srcs = glob(["*.sh", "*.mjs", "*.cjs"]))
sh_binary(
    name = "smoke",
    srcs = ["smoke.sh"],
    data = [":sources", "//:desktop-bundle", "//docs/design:sources", "//tools:virtual-desktop-run.sh", "//tools:x11-driver-source", "//tools:electron-runtime-source", "//tools/task-integration:sources"],
    tags = ["manual"],
)
